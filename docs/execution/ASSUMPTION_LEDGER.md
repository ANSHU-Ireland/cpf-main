# Assumption Ledger

Classification: **A0** presentation · **A1** reversible · **A2** material (validate early) ·
**A3** legal/safety/irreversible (never silently decided — see EXTERNAL_ACTIONS_REQUIRED).

| ID     | Class | Assumption                                                                                                                                                        | Rationale                                                                                                                                                                          | Status   | Revisit trigger                            |
| ------ | ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------ |
| ASM-01 | A1    | Initialize a dedicated git repo at `CPF-Dev/` root.                                                                                                               | Parent `.git` is at `C:/Users/adikr/Desktop` (whole Desktop, personal files, 0 commits). Committing there is wrong/unsafe. Nested repo scopes commits correctly and is reversible. | ACCEPTED | User wants a different repo location.      |
| ASM-02 | A1    | Stack = TypeScript, pnpm workspaces, Node 24 (installed v24.11.1), strict tsconfig, Vitest, ESLint+Prettier.                                                      | Contract's suggested default shape; greenfield repo; all tooling verified present. See ADR-0001.                                                                                   | ACCEPTED | Requirement forces a different stack.      |
| ASM-03 | A1    | Top-level v2.0 source files remain as the immutable baseline; verified copies also live in `docs/source-of-truth/originals/`.                                     | Contract requires preserving original bytes and copying into originals.                                                                                                            | ACCEPTED | —                                          |
| ASM-04 | A2    | Postgres is unavailable locally until Docker/EXT-01 resolved; DB integration tests are authored but conditionally skipped when no `DATABASE_URL`/pg is reachable. | Docker absent (EXT-01). Keeps suite green without deleting/weakening tests.                                                                                                        | ACTIVE   | Docker installed or Postgres URL provided. |
| ASM-05 | A1    | Logical table count = 138; `audit.events_default` is a physical partition (CONFLICT-001).                                                                         | Verified in SQL DDL line 1152.                                                                                                                                                     | ACCEPTED | New DDL partitions change physical count.  |
